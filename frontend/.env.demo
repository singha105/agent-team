# Loaded by `npm run demo` (vite --mode demo).
# Replays frontend/src/demo/fixture.json — a recording of a real run — with no
# backend at all, so the app can be seen with only Node installed.
VITE_DEMO=1
